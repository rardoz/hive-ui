// TODO WE NEED TO PARSE ALL CSS FILES

const style = (s) => {
  if(typeof s === object) {
    //then for each key write some css
  }

  else if(typeof s === string) {
    //then assume this is a file path
  }
}

module.exports = {style}